<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Enline File-Upload</title>
</head>
<body>
    <header>
        <h1>Enline File-Upload</h1>
    </header>
    <form id="file-form"
        action="/upload"
        method="POST"
        enctype="multipart/form-data"
    >
        <input type="file" name="file" />
        <input type="submit" />
    </form>
    <h3>Files:</h3>
    <ul id="files-ul">
        <% fileList.forEach(file => { %>
            <li>
                <span><%= file.date %></span>
                <span><%= file.size / 1000000 %> MB</span>
                <a href="./<%= file.path %>"><%= file.originalname %></a>
            </li>
        <% }) %>
    </ul>   
</body>
</html>